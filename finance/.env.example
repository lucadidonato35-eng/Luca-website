# Copy to .env and edit. .env is gitignored and must never be committed.

# Where the local SQLite database lives. Stays on this machine. Never synced.
DATABASE_PATH=./data/cockpit.db

# Phase 5 only. The app is fully functional without this: the narrative layer
# is a convenience, the deterministic numbers are the product.
# ANTHROPIC_API_KEY=
